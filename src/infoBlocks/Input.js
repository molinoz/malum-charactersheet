import React from "react";

export default function Input(){
    switch(data.form) {
        case 'text':
            return()
        case 'area':
            return()
        case 'number':
            return()
    }
}